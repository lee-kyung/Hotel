<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybats-3-mapper.dtd">
 
<mapper namespace="kr.co.hotel.login.LoginMapper">
	
	<select id="login_ok" resultType="kr.co.hotel.member.MemberVO">
		select * from member where userid=#{userid} and pwd=#{pwd}
	</select>
	
	<!-- <select id="checkCart" resultType="Integer">
		select count(*) from cart where userid=#{param1}
	</select>
	
	<select id="checkCart_userid" resultType="Integer">
		select count(*) from cart
		where (select pcode from cart where userid=#{param1})=(select pcode from cart where userid=#{param2})
	</select>
	
	<update id="cart_userid_change">
		update cart set userid=#{param1} where userid=#{param2}
	</update>
	
	<delete id="cart_del">
		delete from cart where userid=#{param1}
	</delete>
	
	<delete id="cart_del_duplicate">
		delete from cart
		where (select pcode from cart where userid=#{param1})=(select pcode from cart where userid=#{param2})
		and userid=#{param2}
	</delete> -->
</mapper>